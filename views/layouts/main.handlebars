{{!-- delete -- same as in quotes app in class assignment --}}
{{!-- this will show an unordered list of all the submitted New Notes --}}
{{!-- you will be able to have the option to delete the New Note --}}
{{!-- or you will be able to Post a New Note --}}
<ul>
  {{!-- loading newNote template into other templates --}}
  {{#each newNote}}
  	<li>
  		<p><span class="label">ID:</span> {{this.id}}</p>
      <p><span class="label">Title:</span> {{this.title}}</p>
      <p><span class="label">Body:</span> {{this.body}}</p>

      <button data-id="{{this.id}}" class="delquote">Delete</button>

      <a href="/{{this.id}}">Post New Note</a>
  	</li>
  {{/each}}
</ul>